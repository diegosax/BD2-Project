CREATE TABLE "User"
(
  id serial NOT NULL,
  "name" text NOT NULL,
  email text NOT NULL,
  "password" text NOT NULL,
  crm text,
  "type" smallint NOT NULL DEFAULT 0,
  CONSTRAINT id PRIMARY KEY (id)
);